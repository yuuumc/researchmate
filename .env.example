# ============================================================
# 研芯通环境变量模板
# ============================================================
# 复制本文件为 .env 并填入实际值（.env 已在 .gitignore 中，不会入库）
#
# ⚠️ 安全铁律（v3 §翻车点 11）：
#   - DEEPSEEK_API_KEY 必须放服务端（Vercel Project Settings → Environment Variables）
#   - 严禁使用 VITE_DEEPSEEK_API_KEY 前缀（会被打进前端 bundle，公开部署等于公开 key）
#   - 前端只调自己的 /api/chat 代理，DevTools Network 不得出现 api.deepseek.com
# ============================================================

# Vercel serverless function 读取（api/chat.js 用）
# 本地开发：可在 vercel dev 启动时注入，或在 .env 中配置后用 dotenv 加载
DEEPSEEK_API_KEY=sk-your-deepseek-key-here

# 前端配置（可用 VITE_ 前缀，但禁止放任何密钥）
# API 代理地址（空字符串=同源，请求 /api/chat；本地 vercel dev 时留空即可）
VITE_API_BASE=

# 学科解耦（v2.0 升级：运行时切换，不再"必须"）
#   microelectronics（默认）= 微电子（半导体物理 + 长三角微电子院校）
#   cs                       = 计算机（数据结构 + CS 院校）
# v1.5 行为：改这一个变量 + 重新 build = 换专业
# v2.0 行为：URL 加 ?subject=cs 即时切换，无需 build；本变量保留为"第 3 优先级"兜底
# 留空或注释掉 = 用 subjects.json 注册表第一项
VITE_SUBJECT=microelectronics

# 后期自定义 API 接入点（v1 占位，未实现）
# VITE_CUSTOM_API_BASE=

# ============================================================
# Supabase 直连配置（v2.0 多用户 SaaS · 数据层）
# ============================================================
# 直连 + RLS，不走 /api/* 代理
# 真实值在飞书云文档 / 部署环境取，.env 不入库
# 项目 URL 已固定：rgvceoumsmxacswobfcp.supabase.co
VITE_SUPABASE_URL=https://rgvceoumsmxacswobfcp.supabase.co
VITE_SUPABASE_ANON_KEY=
